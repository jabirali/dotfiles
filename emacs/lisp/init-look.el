;;; init-look.el --- Themes, fonts, etc.

;;; Code:
;; Default color theme for new frames.
(load-theme 'modus-vivendi)

;; Default font for the GUI frames.
(set-frame-font "Iosevka Comfy:size=14:weight=light" :frames t)

(provide 'init-look)
;;; init-look.el ends here
