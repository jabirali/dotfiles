# ~/.config/kak/kakrc

# Quickstart:
# - Clone `andreyorst/plug.kak` into ~/.config/plugins/plug.kak;
# - Run `:plug-install` to install all the plugins listed below.

# External modules.
source "%val{config}/plugins/plug.kak/rc/plug.kak"
plug "andreyorst/plug.kak" noload
plug "andreyorst/fzf.kak"
plug "ul/kak-lsp" do %{ cargo install --locked --force --path . }

# FZF integration.
map global user , ':fzf-mode<ret>'

# LSP integration.
lsp-enable
map global user l ':enter-user-mode lsp<ret>'

# Settings.
set global ui_options ncurses_assistant=none
