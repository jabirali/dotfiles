# ~/.config/offlineimap/config

[general]
accounts         = personal
metadata         = ~/.cache/offlineimap
pythonfile       = ~/.config/offlineimap/auth.py
status_backend   = sqlite
autorefresh      = 3
quick            = 9
starttls         = yes
ssl              = yes

[Account personal]
localrepository  = personal-local
remoterepository = personal-remote

[Repository personal-local]
type             = Maildir
localfolders     = ~/mail/personal
nametrans        = lambda x: x.upper() if x == "inbox" else x.capitalize()

[Repository personal-remote]
type             = IMAP
remotehost       = imap.kolabnow.com
remoteuser       = jabirali@switzerlandmail.ch
remotepasseval   = keyring('jabirali@switzerlandmail.ch')
sslcacertfile    = /etc/ssl/certs/ca-certificates.crt
folderfilter     = lambda f: f in ["INBOX", "Archive", "Sent", "Spam", "Trash"]
nametrans        = lambda f: f.lower()
