# This file defines aliases for `fish`, via the `fish-abbrfile` syntax.
# For more information, see: https://github.com/jabirali/fish-abbrfile

# Better UNIX IDE.
vi    vim
vi    subl
cat   bat -p
less  bat -p
find  fd
grep  rg
tree  exa -T
ls    exa

# Shorter commands.
a    sudo apt
ai   sudo apt install
ar   sudo apt autoremove --purge
au   sudo apt update && sudo apt upgrade
as   apt search

b    brew
bb   brew sync
be   open -t ~/.config/brewfile/Brewfile
bi   brew install
br   brew remove
bu   brew upgrade
bs   brew search

c    cd ~/.config

e    vi
e    vim
e    nvim
e    subl

g    git
ga   git add
gb   git branch
gc   git commit
gd   git diff
gdc  git diff --cached
gf   git fetch
gm   git merge
gp   git push
gs   git status
gr   git reset

p    projector

l    ls
l    exa

o    open
o    xdg-open
o    explorer.exe

O    qlmanage -p
