# This file defines aliases for `fish`, via the `fish-abbrfile` syntax.
# For more information, see: https://github.com/jabirali/fish-abbrfile

# Better UNIX IDE.
vi    vim
vi    nvim
cat   bat -p
find  fd
grep  rg
tree  exa -T
ls    exa

# Shorter commands.
a   sudo apt
ai  sudo apt install
ar  sudo apt autoremove --purge
au  sudo apt update && sudo apt upgrade
as  apt search

b   brew
bi  brew install
br  brew remove
bu  brew bundle install --global && brew bundle cleanup --global --force
bs  brew search

g   git
ga  git add
gb  git branch
gc  git commit
gd  git diff
gf  git fetch
gm  git merge
gp  git push
gs  git status
gr  git reset

l   ls
l   exa

o   open
o   xdg-open

v   vi
v   vim
v   nvim

z   zotfile
