# ~/.config/fish/abbrfile:
# This file defines aliases for `fish`, via the `fish-abbrfile` syntax.
# For more information, see: https://github.com/jabirali/fish-abbrfile

# Better UNIX IDE.
cat   bat -p
find  fd
grep  rg
tree  exa -T

# Shorter commands.
b    brew
bb   brew sync
bc   brew clean
be   vim ~/.config/Brewfile
bi   brew install
br   brew remove
bu   brew upgrade
bs   brew search

d    dotfiles

e    emacsclient -nc
ee   tangle

g    git
ga   git add
gb   git branch
gc   git commit
gcm  git commit -m
gca  git commit -am
gd   git diff
gdc  git diff --cached
gf   git fetch
gm   git merge
gp   git pull
gP   git push
gs   git status
gr   git reset

o    open
o    xdg-open
o    explorer.exe

p    ipython

q    qlmanage -p

v    vi
v    vim
