# This file defines aliases for `fish`, via the `fish-abbrfile` syntax.
# For more information, see: https://github.com/jabirali/fish-abbrfile

# Edit with best Vim.
e  vi
e  vim
e  nvim

v  vi
v  vim
v  nvim

# Open in default app.
o  open
o  xdg-open

# Command replacements.
vi    vim
vi    nvim
cat   bat -p
find  fd
grep  rg
tree  exa -T
ls    exa

# Version control.
g   git
ga  git add
gb  git branch
gc  git commit
gd  git diff
gf  git fetch
gm  git merge
gp  git push
gs  git status
gr  git reset

# Package management.
a   sudo apt
ai  sudo apt install
ar  sudo apt autoremove --purge
au  sudo apt upgrade
as  apt search

b   brew
bi  brew bundle install --global
br  brew bundle cleanup --global --force
bu  brew update
bs  brew search

s   snap
si  snap install
sr  snap remove
ss  snap search

f   flatpak
fi  flatpak install
fr  flatpak uninstall
fs  flatpak search

# Miscellaneous.
n   nvim +WikiFzfPages
z   zotfile
